/**
 * Branding in one place. The final name isn't decided yet — change APP_NAME here (and the
 * <title> in index.html) and it updates everywhere.
 */
export const APP_NAME = 'ABA Desk'

/**
 * Optional personal message shown under the logo on the sign-in screen. Leave it empty ('')
 * when the app is offered to other people.
 */
export const DEDICATION = ''
